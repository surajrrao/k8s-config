apiVersion: v1
kind: Service
metadata:
  name: priceinfo-backend-service
  namespace: production
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: gceme
    role: frontend
